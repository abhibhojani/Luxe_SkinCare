# ==========================================
# PostgreSQL Database Configuration
# ==========================================
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/luxclinic}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Abhi@121}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ==========================================
# Owner / Doctor Email Recipient
# ==========================================
# Put the doctor/owner's email address here where new appointment alerts will be received
clinic.owner.email=rutvik1500@gmail.com

# ==========================================
# Outgoing Mail Configuration (Gmail SMTP)
# ==========================================
# 1. Put your clinic sender Gmail address in spring.mail.username
# 2. Generate a 16-character App Password at https://myaccount.google.com/apppasswords
# 3. Paste the App Password into spring.mail.password (no spaces)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:abhibhojani121@gmail.com}
spring.mail.password=${MAIL_PASSWORD:zxjc fuvp gsya wcvt}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# ==========================================
# Server Port
# ==========================================
server.port=${PORT:8080}
